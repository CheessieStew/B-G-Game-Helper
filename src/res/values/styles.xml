<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo">
        <!-- Customize your theme here. -->
    </style>
    <style name="HelpTheme" parent="@android:style/Theme.Holo.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
</resources>
